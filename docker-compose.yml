services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
      target: release
    container_name: sun-bot
    restart: always
    environment:
      - NODE_ENV=production
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"