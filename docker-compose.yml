services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
      target: release
    container_name: sun-bot
    restart: always
    environment:
      - NODE_ENV=production
      - TOKEN=${TOKEN}
      - DBPASSWORD=${DBPASSWORD}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_HOST=${POSTGRES_HOST:-dumbo.feyli.dev}
      - ARCANEDBPASSWORD=${ARCANEDBPASSWORD}
      - OPENAIKEY=${OPENAIKEY}
      - ENVIRONMENT=${ENVIRONMENT}
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"